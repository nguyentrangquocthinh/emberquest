name: ember_quest
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 < 4.0.0'

flutter:
  assets:
    - assets/images/


dependencies:
  flutter:
    sdk: flutter
  flame: ^1.17.0

